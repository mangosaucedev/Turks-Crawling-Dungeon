using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TCD.IO;
//using TCD.IO.Persistence;

namespace TCD
{
    
    public class SaveOperation //: ILoadingOperation
    {
        /*
        private SaveManager saveManager;
        private string gameName;

        public string Name => "Save Operation";

        public string Message => "Saving game...";

        public string Description => Message;

        public bool HasStarted { get; set; }

        public bool IsDone { get; set; }

        public float Progress => saveManager.GetProgress();

        public SaveOperation(string gameName)
        {
            saveManager = ServiceLocator.Get<SaveManager>();
            this.gameName = gameName;
        }

        public IEnumerator Load()
        {
            yield return SaveManager.SaveGameAsRoutine(gameName);
            IsDone = true;
        }
        */
    }
}
